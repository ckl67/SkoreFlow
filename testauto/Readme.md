# Installation

Based on the fact that we are in a monorepo, the dependencies have to be installed as following

```shell

# from root
npm install --save-dev vitest -w testauto/backend
npm install --save-dev prettier eslint @eslint/js -w testauto/backend


```
