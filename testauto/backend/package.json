{
  "name": "tests-backend",
  "type": "module",
  "dependencies": {
    "axios": "^1.15.1",
    "form-data": "^4.0.5"
  },
  "scripts": {
    "run:basics": "bash auto-test.sh",
    "run:clean": "bash auto-test.sh --clean",
    "run:all": "bash auto-test.sh --clean --all",
    "run:kill": "bash auto-test.sh --kill",
    "test": "vitest",
    "test:run": "vitest run",
    "test:watch": "vitest"
  }
}
