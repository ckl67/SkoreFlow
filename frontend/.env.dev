# For dev
# ----------------
# We will not use environement variables (much flexible)
# vite build --mode dev

# Backend address
# The browser downloads the frontend from address below
VITE_API_URL=http://localhost:8080/api/v1

# Will activate the debug panel
VITE_TEST_MODE=true

