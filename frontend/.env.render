# For production
# ----------------
# npm run build --> vite build (packages.json)
# We setup via environement variables !

# Backend address
VITE_API_URL=https://skoreflow.onrender.com/api
# Will activate the debug panel
VITE_TEST_MODE=true
